"""auto-dev-sdk v2 — harness for the auto-dev feature pipeline.

This package is independent of the deferred v0.1 `auto_dev/` archive.
v2 routes harness-owned LLM calls through the packaged shared/vendors
adapter; the harness is still a pure state machine with no in-process LLM.
"""

__version__ = "2.0.0-dev"
